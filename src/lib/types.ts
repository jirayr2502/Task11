export interface IUsers {
    id: number | string;
    name: string;
    surname: string;
    age: number;
    salary: number;
}

export interface IConstrucorForm {
    name: string
    surname: string
    age: number
    salary: number
}

export interface IUser {
    id: number | string
    name: string
    surname: string
    age: string
    salary: string
}
